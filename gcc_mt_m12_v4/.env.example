# SERVICE Configuration - Auto-detected from script Section 4
# PRIMARY SERVICE: Pinecone (vector database)
# SECONDARY SERVICE: AWS (S3 + CloudFront)

# Pinecone Configuration
PINECONE_ENABLED=false
PINECONE_API_KEY=your_pinecone_api_key_here
PINECONE_ENVIRONMENT=us-west1-gcp
PINECONE_INDEX_NAME=compliance-rag

# AWS Configuration
AWS_ENABLED=false
AWS_ACCESS_KEY_ID=your_aws_access_key_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_key_here
AWS_REGION=us-east-1
S3_BUCKET_NAME=compliance-documents
CLOUDFRONT_DISTRIBUTION_ID=your_cloudfront_distribution_id

# Redis Configuration (cache + Celery broker)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# PostgreSQL Configuration (compliance config + audit trail)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=compliance_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_postgres_password_here

# Celery Configuration
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# Offline Mode (for notebooks and testing without external services)
OFFLINE=false

# Logging
LOG_LEVEL=INFO

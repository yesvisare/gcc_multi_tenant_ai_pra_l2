# L3 M13.3: Cost Optimization Strategies - Configuration

# Infrastructure Configuration (All Optional)
# This module works entirely in-memory without external services

# Prometheus (metrics collection) - Optional
PROMETHEUS_ENABLED=false
PROMETHEUS_HOST=localhost
PROMETHEUS_PORT=9090

# StatsD (event recording) - Optional
STATSD_ENABLED=false
STATSD_HOST=localhost
STATSD_PORT=8125

# PostgreSQL (historical data storage) - Optional
POSTGRES_ENABLED=false
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=cost_attribution
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password_here

# Logging
LOG_LEVEL=INFO

# Note: Unlike other L3 modules, this one does NOT require external AI services
# (no OpenAI, Anthropic, Pinecone, etc.)
# All functionality works out-of-the-box using in-memory storage

# Environment Configuration for L3 M14.4: Platform Governance

# Operating mode (default: true, no external services needed)
OFFLINE=true

# Logging
LOG_LEVEL=INFO

# Team Sizing Configuration (optional overrides)
AVG_ENGINEER_SALARY_INR=3000000  # ₹30L per year
CHAMPION_COST_PER_HOUR_INR=2000  # ₹2000 per hour

# Operating Model Thresholds (optional overrides)
SMALL_SCALE_THRESHOLD=10   # < 10 tenants
LARGE_SCALE_THRESHOLD=50   # > 50 tenants

# Self-Service Target (optional override)
SELF_SERVICE_TARGET_PERCENTAGE=80  # 80% tier 1 auto-approval
